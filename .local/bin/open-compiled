#!/bin/sh

# back-end script that opens a compiled file

file="${1}"
ext="${file##*.}"
dir=$(dirname "$file")
base="${file%.*}"

tex_open(){
    [ -f "${base}".pdf ] || base="main"
    setsid -f zathura "${base}".pdf >/dev/null 2>&1 
}

case "${*}" in
    *.tex) tex_open ;;
esac
