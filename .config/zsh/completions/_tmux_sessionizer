#compdef tmux-sessionizer

_tmux-sessionizer() {
  local sessions

  sessions=($(tmux list-sessions -F "#{session_name}" 2>/dev/null))

  _arguments \
    '*:Session name:(${sessions[@]})'
}

_tmux-sessionizer "$@"
