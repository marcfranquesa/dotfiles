# Repository Guidance

## Commit Scopes

For Conventional Commit scopes in this repo, prefer touched-area names such as
`shell`, `git`, `tmux`, `nvim`, `bootstrap`, `docs`, or `agents`. Use `agents`
for `.config/agents` changes and matching Codex/Claude agent links. Omit the
scope when it adds no clarity.
